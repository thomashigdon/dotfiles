start-server
set-option -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

set -g mouse on
#set -g mode-mouse on
#setw -g mouse-select-window on
#setw -g mouse-select-pane on

set-window-option -g mode-keys vi
#set-window-option -g automatic-rename off

set-option -g status-bg white
set-option -g status-fg black
set-option -g status on
#set-window-option -g utf8 on

setw -g aggressive-resize on
set -g history-limit 100000

#unbind p
#bind p paste-buffer
bind b choose-buffer
bind-key -t vi-copy 'v' begin-selection
bind-key -t vi-copy 'y' copy-selection
bind-key -t vi-copy 'r' rectangle-toggle
#bind y run "tmux save-buffer - | reattach-to-user-namespace pbcopy"

set -g set-titles off
#set-option -g default-command "/bin/bash -c '/usr/local/bin/reattach-to-user-namespace $SHELL 2> /dev/null || exec /bin/zsh'"
#set -g default-command "exec /bin/zsh"
set-option -g default-terminal "screen-256color"
set -g @colors-solarized 'dark'

bind-key u send-keys cat space /proc/ath_rincon/status && ifconfig space br0 && grep space SSID space /proc/ath_rincon/station enter
#bind-key i send-keys tail space -f space /opt/log/netstartd.log & enter
bind-key o send-keys while space true enter do space dmesg space -c enter sleep space 1 enter done & enter
bind-key y send-keys killall space -USR1 space udhcpc
bind-key h send-keys tail space -f space /opt/log/anacapa.trace & enter

run-shell "powerline-daemon -q"
source "/usr/local/lib/python2.7/dist-packages/powerline/bindings/tmux/powerline.conf"

#set -g status-left-length 52
#set -g status-right-length 451
#set -g status-fg white
#set -g status-bg colour234
#set -g window-status-activity-attr bold
#set -g pane-border-fg colour245
#set -g pane-active-border-fg colour39
#set -g message-fg colour16
#set -g message-bg colour221
#set -g message-attr bold
#set -g status-left '#[fg=colour235,bg=colour252,bold] ❐ #S #[fg=colour252,bg=colour238,nobold]⮀#[fg=colour245,bg=colour238,bold] #(whoami) #[fg=colour238,bg=colour234,nobold]⮀'
#set -g window-status-format "#[fg=white,bg=colour234] #I #W "
#set -g window-status-current-format "#[fg=colour234,bg=colour39]⮀#[fg=colour25,bg=colour39,noreverse,bold] #I ⮁ #W #[fg=colour39,bg=colour234,nobold]⮀"
#
#run-shell "powerline-daemon -q"
#set-option -ga update-environment ' DBUS_SESSION_BUS_ADDRESS'
#if-shell 'test -z "$POWERLINE_CONFIG_COMMAND"' 'set-environment -g POWERLINE_CONFIG_COMMAND powerline-config'
#
## Don't version-check for this core functionality -- anything too old to
## support these options likely won't work well with powerline
#set -g status on
##set -g status-utf8 on
#set -g status-interval 2
#set -g status-left-length 20
#set -g status-right '#(eval $POWERLINE_COMMAND tmux right -R pane_id=`tmux display -p "#D"`)'
#set -g status-right-length 150
#set -g window-status-format "#[fg=colour244,bg=colour234]#I #[fg=colour240] #[default]#W "
#set -g window-status-current-format "#[fg=colour234,bg=colour31]#[fg=colour117,bg=colour31] #I  #[fg=colour231,bold]#W #[fg=colour31,bg=colour234,nobold]"
#
## Legacy status-left definition to be overwritten for tmux Versions 1.8+
#set -g status-left '#[fg=colour16,bg=colour254,bold] ❐ #S #[fg=colour254,bg=colour234,nobold]#(eval $POWERLINE_COMMAND tmux left)'

# Simplify tmux version checking by using multiple config files. Source these
# config files based on the version in which tmux features were added and/or
# deprecated. By splitting these configuration options into separate files,
#run-shell 'eval $POWERLINE_CONFIG_COMMAND tmux source'
# vim: ft=tmux
set -g status-bg colour40
setw -g window-status-current-bg colour40

bind -n C-t new-window -a
bind -n S-left  prev
bind -n S-right next
bind -n S-C-left  swap-window -t -1
bind -n S-C-right swap-window -t +1

bind -n M-F11 set -qg status-bg colour25
bind -n M-F12 set -qg status-bg colour40
bind -n S-up \
	send-keys M-F12 \; \
	set -qg status-bg colour25 \; \
	unbind -n S-left \; \
	unbind -n S-right \; \
	unbind -n S-C-left \; \
	unbind -n S-C-right \; \
	unbind -n C-t \; \
	set -qg prefix C-b
bind -n S-down \
	send-keys M-F11 \; \
	set -qg status-bg colour40 \; \
	bind -n S-left  prev \; \
	bind -n S-right next \; \
	bind -n S-C-left swap-window -t -1 \; \
	bind -n S-C-right swap-window -t +1 \; \
	bind -n C-t new-window -a -c "#{pane_current_path}" \; \
	set -qg prefix C-a

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

# Other examples:
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
